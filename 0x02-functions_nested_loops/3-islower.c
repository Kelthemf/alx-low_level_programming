#include "main.h"

/**
 * _islower - checks if a character is lowercase
 * @c: character to be scrutinized
 * Return: 1 if true, 0 if false
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
